{
  "name": "docker-storage-test",
  "version": "1.0.0",
  "description": "This script test the two storage types available in docker",
  "main": "stressTest.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Meth Munindradasa",
  "license": "ISC",
  "dependencies": {
    "mongodb": "^6.2.0",
    "perf_hooks": "^0.0.1"
  }
}
